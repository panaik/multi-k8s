sudo: required
services:
  - docker

before_install:
  - docker build -t panaik/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run panaik/react-test npm test -- --coverage

after_success:
  - docker build -t panaik/multi-client ./client
  - docker build -t panaik/multi-nginx ./nginx
  - docker build -t panaik/multi-server ./server
  - docker build -t panaik/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push panaik/multi-client
  - docker push panaik/multi-nginx
  - docker push panaik/multi-server
  - docker push panaik/multi-worker
